# Darwin Core Archive export configuration.
# Layout: Event-core sampling-event star schema (one core + three extensions),
# joined by eventID. Each diary entry is an Event; each observation an Occurrence.
publisher: Laubmann KG Project
license: CC-BY-4.0
core: event
extensions:
  - occurrence
  - measurement_or_fact
  - multimedia
