# w3id.org/laubmann-kg

Permanent identifiers for the **Laubmann Knowledge Graph** (HistOrniGraph): an RDF
knowledge graph and Darwin Core Archive built from the ornithological field
diaries of Alfred Laubmann (Bavaria, 1917–1965, 34 volumes).

| IRI | resolves to |
|---|---|
| `https://w3id.org/laubmann-kg/ontology` (namespace `…/ontology#`, prefix `lkg`) | OWL ontology — Turtle for RDF clients, pyLODE documentation for browsers |
| `https://w3id.org/laubmann-kg/ontology/<x.y.z>` | versioned ontology (git tag `v<x.y.z>`) |
| `https://w3id.org/laubmann-kg/vocabularies` | SKOS concept schemes (controlled values) |
| `https://w3id.org/laubmann-kg/shapes` | SHACL shapes |
| `https://w3id.org/laubmann-kg/context.jsonld` | JSON-LD context |
| `https://w3id.org/laubmann-kg/data/<id>` | instance IRIs (entries, observations, taxa, places, persons, runs) |

Source repository: https://github.com/Maelkolb/laubmann-kg_TP  
Contact / maintainer: GitHub user **Maelkolb** (add e-mail here for the PR).

*Status: prepared, not yet submitted — the ontology is still being finalized.
To submit: copy this folder to a fork of https://github.com/perma-id/w3id.org
and open a pull request. Enable GitHub Pages for the repository (branch `main`,
folder `/docs`) before submitting so the HTML targets resolve.*
